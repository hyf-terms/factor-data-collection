# 毛利盈利因子复现说明

## 论文指标

Novy-Marx（2013）的核心指标是毛利盈利能力（gross profitability），不是销售毛利率：

```text
毛利润 = 营业收入 - 营业成本
毛利盈利因子 = 毛利润 / 期末总资产
```

对应新准则 PIT 字段：

```text
(new_pit_income.REVENUE - new_pit_income.COGS)
/ new_pit_balance.T_ASSETS
```

分母使用总资产，是为了衡量企业每投入一元资产能够产生多少毛利润。论文使用上一
财年的年报数据，每年 6 月末分组并持有一年；剔除金融企业，按毛利盈利因子排序，
做多高盈利组合、做空低盈利组合，并使用市值加权收益检验盈利溢价。

本项目默认保留论文的“年报”口径，但将信息可用时点改造成严格 PIT 版本，以便与
日频标签测试：

- 只用合并年报（`MERGED_FLAG='1'`、`REPORT_TYPE='A'`）。
- 只用当前报告期记录。
- 利润表与资产负债表都公开后才计算。
- 09:30 前公告可在当日使用，其余公告从下一交易日使用。
- 修订公告只从修订后的可用日生效。
- 每日用当时最新的已公开年报值，不按 `END_DATE` 提前回填。
- 金融企业需在合并 BARRA 行业数据后剔除；本程序不猜测行业字段。

## Notebook 使用

```python
from gross_profitability_factor import build_daily_gross_profitability

income = pd.read_parquet(
    r"C:\Users\hyf\Desktop\因子\data\new_pit\new_pit_income"
)
balance = pd.read_parquet(
    r"C:\Users\hyf\Desktop\因子\data\new_pit\new_pit_balance"
)
universe = pd.read_parquet(
    r"C:\Users\hyf\Desktop\因子\label.parquet",
    columns=["TRADE_DATE", "SECURITY_ID"],
)

factor = build_daily_gross_profitability(
    income=income,
    balance=balance,
    universe=universe,
    annual_only=True,
)

factor.to_parquet(
    r"C:\Users\hyf\Desktop\因子\gross_profitability.parquet",
    index=False,
)
```

`build_daily_gross_profitability()` 返回的审计字段：

| 字段 | 含义 |
|---|---|
| `GROSS_PROFITABILITY` | 论文原始因子值 |
| `GROSS_PROFITABILITY_W` | 每日截面 1%/99% 缩尾后的值 |
| `GROSS_PROFITABILITY_RANK` | 每日截面百分位排名 |
| `END_DATE` | 当日实际使用的财报截止日，用于审计 PIT |

后续与 `label.parquet`、`barra_diy.parquet` 按
`TRADE_DATE + SECURITY_ID` 合并，对 `GROSS_PROFITABILITY_W` 做 BARRA
残差化，再计算残差与标签的日截面 Spearman IC。

命令行默认输出则只包含：

```text
TRADE_DATE, SECURITY_ID, gross_profitability
```

其中 `gross_profitability` 是每日缩尾后的值，可以直接作为
`factors_neus_only.py` 的输入，不会把 `END_DATE` 等审计字段误当成因子。

## 命令行使用

下载程序采用默认目录时：

```powershell
python gross_profitability_factor.py `
  --pit-dir "C:\Users\hyf\Desktop\因子\data\new_pit" `
  --universe "C:\Users\hyf\Desktop\因子\label.parquet" `
  --output "C:\Users\hyf\Desktop\因子\factors.parquet" `
  --audit-output "C:\Users\hyf\Desktop\因子\gross_profitability_audit.parquet"
```

也可用 `--income`、`--balance` 分别指定单个Parquet文件或其他数据集目录。
`--audit-output` 可以省略。
`--all-reports` 会使用一季报、半年报和三季报，但累计利润表需要进一步转成 TTM
才适合比较；该选项属于扩展研究口径，不是论文的严格复现口径。

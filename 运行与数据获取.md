# 运行与数据获取

## 1. 环境

```powershell
python -m venv .venv
& .\.venv\Scripts\python.exe -m pip install -r requirements.txt
& .\.venv\Scripts\python.exe -m pytest -q
```

PowerShell禁止执行 `Activate.ps1` 时无需修改系统策略，直接调用 `.venv\Scripts\python.exe` 即可。

## 2. 本地配置

仓库不保存真实凭据。二选一创建本地配置：

- 将 `new_pit.env.example` 复制为 `.env`；或
- 将 `new_pit_db_local.example.py` 复制为 `new_pit_db_local.py`。

两种真实配置均已被 `.gitignore` 排除。数据库用于读取公司数据源；研究数据最终保存为本地Parquet，不要求使用ArcticDB。

## 3. 下载数据

### 三张新准则PIT表

```powershell
python download_new_pit_mysql.py
```

| 报表 | 本地目录 |
|---|---|
| 资产负债表PIT | `data/new_pit/new_pit_balance` |
| 利润表PIT | `data/new_pit/new_pit_income` |
| 现金流量表PIT | `data/new_pit/new_pit_cashflow` |

### 任意已核准MySQL表

先用 `discover_alternative_financial_tables.py` 或 `discover_priority_financial_structures.py` 检查表、披露日期字段和覆盖率，再下载最小字段集：

```powershell
python download_mysql_table_parquet.py `
  --config-dir "C:\Users\hyf\Desktop\因子" `
  --table fdmt_adt_opn_n `
  --date-column PUBLISH_DATE `
  --start-date 2016-01-01 `
  --end-date 2026-08-13 `
  --output-dir "C:\Users\hyf\Desktop\因子\data\alternative_financial" `
  --columns PARTY_ID TICKER_SYMBOL PUBLISH_DATE END_DATE DM_OPN_TYPE UPDATE_TIME
```

下载器按日期分片并生成 `manifest.json`，已存在的分片不会重复下载。

## 4. 构造候选因子

各轮构造程序读取PIT或补充数据，输出至少包含：

```text
TRADE_DATE, SECURITY_ID, factor_1, factor_2, ...
```

重要程序分组：

- 基础复现：`gross_profitability_factor.py`、`pead_sue_factor.py`、`quarterly_f_score.py`、`mohanram_g_score.py`、`ch_factor_models.py`；
- 原始字段与全季度：`raw_q1_minimal_factor_search.py`、`all_quarter_raw_factor_search.py`、`coverage_literature_factor_search.py`；
- 文献稠密与盈利状态：`dense_literature_profitability_factors.py`、`seventh_round_literature_factors.py`、`eighteenth_round_profitability_state_factors.py`、`nineteenth_round_quality_state_factors.py`、`forty_third_profitability_persistence_confirmation.py`；
- 独立财务机制：第20—69轮对应英文序号程序；
- 最近结构数据：`round65_69_priority_structure_factors.py`。

不要直接覆盖历史 `factors.parquet`。单个因子可保存为独立Parquet，并通过 `--factor-columns` 指定测试。

## 5. 自然缺失诊断与稠密化

时段因子先运行诊断：

```powershell
python factors_neus_only2.py `
  --factors .\artifacts\candidate_sparse.parquet `
  --barra "C:\Users\hyf\Desktop\因子\barra_diy.parquet" `
  --label "C:\Users\hyf\Desktop\因子\label.parquet" `
  --output-dir .\artifacts\sparse_diagnostic `
  --barra-profile notebook --preprocess mad `
  --cross-section-policy skip
```

这里的 `skip` 仅用于诊断自然缺失和方向，不能作为正式IC。缺失率低于10%、无整日空值的候选可用 `prepare_strict_neutral_fill.py` 或相应轮次的填充程序做每日中位数填充，再检查恒定日。

## 6. 正式测试

```powershell
python factors_neus_only2.py `
  --factors .\candidate_dense.parquet `
  --barra "C:\Users\hyf\Desktop\因子\barra_diy.parquet" `
  --label "C:\Users\hyf\Desktop\因子\label.parquet" `
  --factor-columns factor_a factor_b `
  --output-dir .\artifacts\strict_test `
  --barra-profile notebook --preprocess mad `
  --cross-section-policy strict
```

主要输出：

- `strict_factor_eligibility.csv`：完整股票日、缺失和方差预检；
- `factor_quality_diagnostics.csv`：逐年质量；
- `factors_neutralized.parquet`：Barra残差；
- `daily_ic.parquet`：每日IC；
- `ic_summary.csv`：平均IC、ICIR、胜率与分段指标。

## 7. 达标因子贪心筛选

将各正式测试目录的 `daily_ic.parquet` 传入：

```powershell
python greedy_ic_factor_selector.py `
  --daily-ic .\round7\daily_ic.parquet .\round18\daily_ic.parquet .\round43\daily_ic.parquet `
  --output-dir .\factor_selection `
  --ic-threshold 0.035 `
  --correlation-threshold 0.85
```

程序只使用 `neutral` 版本，输出 `greedy_selection.csv`、`daily_ic_abs_correlation.csv` 和 `selected_daily_ic.parquet`。

## 8. 新增因子的交付检查

1. 公式、字段、方向和PIT可用日写清楚；
2. 不读取label构造因子；
3. 时段数据先诊断后填充；
4. 填充前缺失率原则上不超过10%；
5. `strict` 覆盖所有日期和共同股票池；
6. IC达到0.03才列潜在，达到0.035才进相关性池；
7. 更新 `有效因子与最终结果.md`，不要新增逐轮流水账Markdown；
8. 运行全部测试后再提交。

# 单季度财务指标 PIT 下载与因子参考

## 数据选择

通联数据库中存在三张容易混淆的表：

| 表 | 字段数 | 公告时间 | 用途 |
|---|---:|---|---|
| `fdmt_indi_q` | 50 | 无 | 旧版单季度指标，仅做字段对照 |
| `fdmt_der_q` | 9 | 无 | 旧版单季度衍生值，仅做字段对照 |
| `fdmt_main_data_q_pit` | 125 | 有 `PUBLISH_DATE` | 新准则严格 PIT 因子输入 |

旧表只有报告截止日和数据库更新时间。数据库更新时间不是投资者在历史时点可以获得数据的
时间，不能据此回填历史因子。因此下载程序默认使用
`fdmt_main_data_q_pit`，并保留公告、报告期和修订记录。

## 下载方法

程序读取被 Git 忽略的 `new_pit_db_local.py`，不会把数据库账号写入仓库：

```powershell
python download_quarterly_financial_indicators.py
```

默认设置：

- 公告日期：2016-01-01 至运行当天；
- 每92天一个查询分块；
- 已存在分块自动跳过；
- 只保留公告时点仍属于A股、且为合并口径的记录；
- 输出目录：
  `data/quarterly_financial_indicators/quarterly_financial_indicator_pit`；
- 字段字典：
  `data/quarterly_financial_indicators/quarterly_financial_indicator_fields.csv`。

只检查权限和字段：

```powershell
python download_quarterly_financial_indicators.py --check-only
```

指定区间或重新下载：

```powershell
python download_quarterly_financial_indicators.py `
  --start-date 2016-01-01 `
  --end-date 2026-07-31 `
  --chunk-days 92
```

## 本地下载结果

本次实际下载公告日期为2016-01-01至2026-07-31，校验结果如下：

- 43个不跨自然年的Parquet分区；
- 242,119行、127列；
- 5,761个证券ID；
- Parquet合计187,465,524字节，约179 MiB；
- 实际公告日期为2016-01-08至2026-07-31；
- PIT事件重复数为0；
- 字段字典包含源表全部125个字段。

数据中可见报告截止日早于2016年的记录。这些记录是2016年以后对旧报告的修订，并非下载
区间错误；因子映射仍应使用其实际 `PUBLISH_DATE`。

## PIT 使用规则

1. 因子生成必须按 `PUBLISH_DATE` 向后映射，不能按 `END_DATE` 回填。
2. 表只有公告日期、没有公告时刻。为避免盘中未来函数，建议从公告日的下一个交易日开始使用。
3. 同一报告期可能有修订记录。映射时保留历史公告事件，但迟到的旧报告不得覆盖已发布的
   更新报告。
4. `IS_NEW` 是数据库当前状态字段，不能用它删除历史修订事件。
5. 候选生成程序不得读取 `label.parquet`；标签只在独立 IC 测试阶段使用。

## 优先因子方向

以下指标可以直接减少重复计算，适合作为后续因子挖掘输入。

### 1. 现金盈利质量

- `N_CF_OPA_NIA`：经营现金流/净利润；
- `N_CF_OPA_R`：经营现金流/营业收入；
- `CFSGS_R`：销售收现/营业收入；
- `N_CF_OPA_NIA_YOY`、`N_CF_OPA_NIA_QOQ`：现金含量变化。

建议测试截面排名、同比变化，以及“高现金含量 + 高扣非利润增长”复合。

### 2. 扣非盈利与非经常损益

- `NI_ATTR_P_CUT`、`NI_ATTR_P_CUT_YOY`、`NI_ATTR_P_CUT_QOQ`；
- `NI_CUT_NI`：扣非归母利润/归母利润；
- `NR_PROFIT_LOSS`：非经常性损益。

重点比较扣非增长与未扣非增长的差值，识别利润增长是否依赖一次性项目。

### 3. 利润率与成本结构

- `GROSS_MARGIN`、`NP_MARGIN`、`ROE`、`ROA`；
- `COGS_TR`、`PERIOD_EXP_TR`、`SELL_EXP_TR`、`ADMIN_EXP_TR`；
- `R_D_EXP_TR`：研发费用/营业总收入。

建议优先使用同比改善、连续两个季度改善和行业内百分位，避免直接重复测试静态水平。

### 4. 收入—利润—现金流联合增长

- `REVENUE_YOY`、`GROSS_PROFIT_YOY`；
- `NI_ATTR_P_CUT_YOY`；
- `N_CF_OPA_YOY`、`C_FR_SALE_G_S_YOY`。

可以构造收入、扣非利润和现金流三者同向的联合惊喜，或对增长不一致程度取负值。

### 5. 经营与偿债质量

- `N_CF_OPA_CL`、`N_CF_OPA_LIAB`、`N_CF_OPA_ID`；
- `N_CF_OPA_ND`、`N_CF_OPA_DEBT_DUE`；
- `AR_R`、`NR_AR_R`、`AR_REC_R`。

这些字段适合测试现金偿债能力改善、应收占收入下降以及回款率改善。

最终是否有效仍以和 `label.parquet` 合并、Barra 逐日残差化后的每日 Spearman IC、
分期稳定性和有效覆盖天数为准。

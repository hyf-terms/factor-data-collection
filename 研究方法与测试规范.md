# 研究方法与测试规范

## 1. 数据与PIT原则

因子只能使用交易日当时已经披露的信息。财务记录至少保留证券、报告期、实际披露时间及修订版本；因子从披露后的首个可交易日开始生效。后续版本只能在其实际披露时间之后覆盖旧值，不能用最终修订值回填历史。

基础数据包括新准则资产负债表、利润表和现金流量表PIT，以及审计、借款、合同、子公司、分析师等补充表。数据库只是数据来源，本地研究统一保存为分区Parquet。

## 2. 稠密化硬性要求

正式因子的目标不是“有事件时能算IC”，而是可以在完整量化股票池每天产生横截面值。

### 2.1 共同股票池

以当日Barra暴露和有效标签的交集作为参考股票池。因子文件必须满足：

- 主键为 `TRADE_DATE + SECURITY_ID`，且唯一；
- 参考池中的每一行均存在因子记录；
- 每个交易日无NaN、无无穷值；
- 每日至少两个经济上可区分的值；
- 横截面方差不能仅为浮点噪声。

任一交易日不满足条件，`strict` 模式拒绝整列并记录在 `strict_factor_eligibility.csv`，不能通过跳过该日计算正式IC。

### 2.2 缺失率与填充

- 正式稠密候选在填充前的全样本缺失率原则上不超过10%。
- 时段数据必须先保留自然缺失完成稀疏诊断，再决定是否填充；稀疏IC不作为正式成绩。
- 公司已有同类披露、但当日缺少该字段时，可用当日截面中位数作中性填充。填充不改变已有观测，也不构造排名R。
- 只有明确表示“未披露即不存在”的点时余额项目才能按零处理；事件、合同和公告没有记录不能自动解释为零。
- 因历史窗口不足导致的早期整日无值，不能用整日零填充，因为会制造恒定横截面。
- 填充后再次检查整日空值、恒定日和低方差日。

`factors_neus_only.py` 会跳过无效横截面，只能复现历史条件IC。新增因子及最终分类必须使用 `factors_neus_only2.py --cross-section-policy strict`。

## 3. 正式中性化与IC

### 3.1 每日预处理

每日对因子做Notebook一致的平滑3倍MAD缩尾。MAD为零时不删除因子，而是交由严格横截面诊断判断该日是否可用。标准化Z仅用于复合时解决量纲差异，不等同于百分位排名。

### 3.2 Barra截面回归

对每个交易日分别估计：

```text
factor_i = intercept + style_i × beta_style + industry_i × beta_industry + residual_i
```

Notebook口径使用指定Barra风格暴露、行业哑变量和常数项。OLS残差代表剔除规模、价值、盈利、成长、杠杆、动量及行业等已知风险后的因子信息。程序检查残差与回归设计矩阵的正交误差。

### 3.3 IC定义

每日IC为中性化残差与 `LABEL` 的截面Spearman相关系数，`LABEL` 表示T+1买入、T+11卖出的10个交易日收益。最终报告使用每日IC的时间均值，同时输出IC标准差、ICIR、正IC日占比、近五年IC及有效股票数。

分类标准：

- `|平均IC| >= 0.035`：有效；
- `0.030 <= |平均IC| < 0.035`：潜在有效；
- 低于0.030：不进入相关性筛选；
- 训练期达标但验证期或留出期明显衰减的，只能作为研究线索。

## 4. 构造与防过拟合规则

- 因子公式不得读取标签；参数只能由经济含义、文献公式或事先冻结的训练期确定。
- 不默认限制Q1、60日窗口或只保留部分公司。
- 新独立因子不能通过对现有最佳因子正交后再回加来“制造”达标；复合必须明确区分独立信号与同族变体。
- 权重网格只在训练期选择，验证期与留出期不得继续调参。
- 全期提升不足0.001、验证期没有同步改善时，不替换原基准。
- 稀疏事件出现极高IC但有效日很少时，只记录诊断，不登记为有效因子。

## 5. 因子组筛选与贪心算法

只有严格IC达到0.035的因子进入候选池。相关性采用各因子每日IC10序列的绝对Pearson相关系数，而不是只比较单个平均IC。

默认贪心步骤：

1. 按 `|平均IC10|` 从高到低排序；
2. 选择排名第一的因子；
3. 依次考察剩余因子；
4. 只有候选与所有已选因子的绝对相关性都严格低于0.85时才加入；
5. 输出入选、拒绝原因、阻挡因子和相关矩阵。

该算法优先保留最强因子、计算简单且结果可解释，但具有顺序依赖，不保证组合数量最大。若目标是最大化因子数量，应额外做图上的最大独立集搜索；即便如此，接近0.85边界的组合也不能视为真正低相关。更稳健的生产阈值建议使用0.70—0.80。

## 6. 研究流程

```text
PIT字段审计 → 经济公式构造 → 自然缺失诊断 → 缺失率≤10%
→ 中性填充 → 全日期/全股票严格预检 → Barra残差化 → 每日IC
→ IC门槛 → 每日IC相关性 → 贪心选择 → 分期与未来数据验证
```

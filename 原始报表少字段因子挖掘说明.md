# 原始报表少字段因子挖掘说明

## 目的

`raw_q1_minimal_factor_search.py` 只读取新准则三张原始报表PIT数据，不依赖通联预计算财务指标，也不读取标签。每个基础指标尽量只用1—4种原始字段，再对基础指标的每日横截面百分位排名做少量复合。

## 严格PIT处理

1. 仅保留 `IS_CURRENT_PERIOD=True`、`REPORT_TYPE=Q1` 且报告期为3月31日的记录。
2. 利润表、现金流量表和资产负债表按证券、报告期、实际披露时刻精确合并。
3. 同比计算使用“本次公告时刻之前已经公开的去年一季报最新修订”，不会读取后来才出现的修订。
4. 09:30前披露可在当日使用，09:30后披露从下一交易日使用。
5. 每个Q1事件最多保留60个交易日；迟到的旧报告修订不会覆盖较新的财务季度。

## 主要基础指标

| 指标 | 公式 | 原始字段种类 |
|---|---|---:|
| 毛利率 | `(营业收入-营业成本)/营业收入` | 2 |
| 毛利率同比变化 | 当期毛利率－去年Q1毛利率 | 2 |
| 现金利润率 | 经营现金流/营业收入 | 2 |
| 现金利润转化 | 经营现金流/归母净利润绝对值 | 2 |
| 回款率 | 销售商品收到现金/营业收入 | 2 |
| 应计质量 | `(经营现金流-归母净利润)/总资产` | 3 |
| 营运资本压力 | `-(应收+存货-应付)/总资产` | 4 |
| 低资本开支 | `-购建固定资产现金/总资产` | 2 |
| 资产周转率 | 营业收入/总资产 | 2 |
| 同比增长 | 收入、利润、经营现金流、销售现金、毛利、研发的Q1同比 | 每项1—2 |

当前已测试版本先对基础指标逐日缩尾并转成百分位排名，再计算均值或“均值－标准差”。这里的“raw”只表示输入来自原始报表；百分位排名 `R` 是衍生变换，不能称为原始数据或原始值。

后续构造采用“原始值优先”双版本口径：先保留由PIT原始字段直接计算、仅做必要异常值处理的 `raw_value`；确需统一量纲时再单独生成 `ranked_value`。两种版本分别进行Barra中性化与IC测试，不再用排名版代表原始值版。通联已定义的财务指标视为具有直接依据，但仍与原始报表复算值并列核对。

## 运行

```powershell
& "C:\Users\hyf\Desktop\因子\.venv\Scripts\python.exe" `
  "C:\Users\hyf\Desktop\因子\raw_q1_minimal_factor_search.py"
```

默认输出为：

`C:\Users\hyf\Desktop\因子\factor_components\raw_q1_minimal_candidates.parquet`

生成后用 `factors_neus_only.py` 与Barra、label合并，逐日Barra残差化并计算Spearman IC。实际测试结果完成后会补充到本说明。

## 2026-07-31测试结果

测试使用当前 `label.parquet`，先逐日对全部Barra风格与行业暴露做OLS残差化，再计算残差因子与label的逐日Spearman IC。共生成并测试26个候选。

| 因子 | 中性化平均IC | ICIR | 结论 |
|---|---:|---:|---|
| Q1归母净利润同比增长 | 0.04273 | 0.917 | 已满足条件 |
| 增长现金广度复合 | 0.03972 | 0.891 | 已满足条件 |
| Q1毛利润同比增长 | 0.03897 | 0.818 | 已满足条件 |
| 研发增长效率复合 | 0.03870 | 0.984 | 已满足条件 |
| 毛利率改善与现金确认 | 0.03773 | 0.865 | 已满足条件 |
| 增长一致性 | 0.03714 | 0.908 | 已满足条件 |
| 成本纪律与毛利增长 | 0.03562 | 0.799 | 已满足条件 |
| 利润现金确认 | 0.03280 | 0.824 | 潜在有效 |
| Q1营业收入同比增长 | 0.03239 | 0.746 | 潜在有效 |

其中最简且表现最好的是 `q1_raw_profit_growth_60d`：只需要归母净利润一个字段的当期Q1值和当时可见的去年Q1值。其训练期、验证期、留出期IC分别为0.04852、0.02973、0.03664。增长现金广度的同期结果为0.04627、0.02224、0.03619；研发增长效率为0.04761、0.02247、0.04200。

全部日频IC、汇总和分期稳健性结果位于 `factor_test_output_raw_q1_minimal`。这批因子只在Q1公告后的60个交易日内有效，因此“有效天数”少于全年交易日数是设计结果，并非数据缺失。
